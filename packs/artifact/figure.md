# Artifact Pack：图表

正式结果图由 MATLAB 读取两类标准工作簿生成。每图必须记录源工作簿、工作表、MATLAB 脚本、图号和正文结论。

默认图窗可见并保留；不批量自动导出。人工调整后调用可选导出函数，将正式图保存到 `figures/`，可编辑源保存到 `figures_editable/`。

机理图根据题目选择 GeoGebra、PPT、draw.io、SVG、TikZ 或 MATLAB；禁止通用“输入—模型—输出”模板。
