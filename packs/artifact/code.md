# Artifact Pack：代码

Python 求解代码必须包含输入检查、随机种子、模型、约束、求解、验证和两类标准 Excel 工作簿输出；不得默认导入 Matplotlib/Seaborn 或生成正式论文图。

MATLAB 绘图代码必须读取 `结果数据表/问题X/问题X结果数据/` 中的标准工作簿，不得重新计算核心结果。默认保留可见图窗，导出显式触发。

代码文件在具体项目中使用中文命名，例如 `问题一求解.py`、`问题一敏感性与鲁棒性.py`、`问题一结果绘图.m`。
