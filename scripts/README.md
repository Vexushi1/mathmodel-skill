# Scripts

- `lint_skill.py`：检查核心文件、活动区旧引用、路径和版本一致性。
- `hsk_check_artifact.py`：检查建模交付物。
- `hsk_pack_submission.py`：打包提交产物。
- `render_paper.py`：Markdown/LaTeX 渲染辅助；使用前按项目目录检查配置。

旧评分、下载与语料处理脚本位于 `legacy/`，不属于默认运行链路。
