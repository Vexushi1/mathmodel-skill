interface:
  display_name: "HSK 数学建模 Skill"
  short_description: "Python求解、标准Excel结果表、MATLAB科研绘图、DOCX草稿与LaTeX终稿"
  default_prompt: "Read REPOSITORY_INDEX.md, core/hsk_core_policy.md and core/workflow_router.yaml, then load only the relevant modules and packs. Follow core/output_contract.yaml and core/workbook_schema.yaml. Python solves and writes the two validated per-question workbooks; MATLAB reads them to create formal result figures."
