---
name: mathmodel-skill
description: Plugin discovery shim for the HSK modular mathematical-modeling workflow.
---

Read `../../SKILL.md`, `../../core/hsk_core_policy.md`, and `../../core/workflow_router.yaml`. Load only the selected modules and packs. Python owns solving and the two standard per-question Excel workbooks; MATLAB owns formal result figures. Resolve all paths relative to `../..` and never load `legacy/` by default.
