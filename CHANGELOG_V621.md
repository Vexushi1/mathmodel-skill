# v6.2.1 变更摘要

- 保留 v6.2.0 的六模块、题型包、竞赛包和交付包架构；
- Python 正式限定为求解与两类标准 Excel 工作簿输出；
- MATLAB 正式限定为读取工作簿绘制结果图，默认保留可见图窗；
- 每问统一为 `结果数据表/问题X/问题X结果数据/`；
- 固定输出 `问题X求解结果.xlsx` 与 `问题X敏感性与鲁棒性结果.xlsx`；
- 约束检查、多算法、明细数据和绘图底层数据进入中文工作表；
- 新增 MATLAB 读取、科研样式、可选导出和题目专属结构图模板；
- LaTeX 路由接入编译质量模块；
- 新增插件封装、职责分离测试和结果路径测试；
- 旧 Python 正式绘图样式移入 `legacy/`。
