# Agent instructions

Read `core/hsk_core_policy.md` and `core/workflow_router.yaml` first. Load only relevant modules and packs. Python solves and writes the two standard per-question Excel workbooks under `结果数据表/问题X/问题X结果数据/`; MATLAB reads them to produce formal result figures. Do not load `legacy/` by default.
